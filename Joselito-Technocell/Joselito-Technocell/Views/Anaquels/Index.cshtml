@model IEnumerable<Joselito_Technocell.Models.Anaquel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InventotyId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InventotyId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AnaquelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnaquelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnaquelId })
        </td>
    </tr>
}

</table>
